# QwenImg 快速开始指南

## 🚀 5分钟快速上手

### 前置要求

- Python 3.8+
- Node.js 16+
- DashScope API Key（[获取地址](https://dashscope.console.aliyun.com/apiKey)）

### 一键启动（推荐）

```bash
# 1. 克隆项目
git clone https://github.com/cclank/qwenimg.git
cd qwenimg

# 2. 配置环境变量（设置你的API Key）
export DASHSCOPE_API_KEY="your_api_key_here"

# 3. 启动开发服务器
./start_dev.sh
```

脚本会自动：
- ✅ 安装后端依赖
- ✅ 安装前端依赖
- ✅ 启动后端服务（8000端口）
- ✅ 启动前端服务（3000端口）

### 访问应用

打开浏览器访问：**http://localhost:3000**

- 前端页面：http://localhost:3000
- API文档：http://localhost:8000/api/docs
- ReDoc文档：http://localhost:8000/api/redoc

### 停止服务

```bash
./stop_dev.sh
```

---

## 📝 手动启动

### 步骤1：启动后端

```bash
# 进入后端目录
cd backend

# 安装依赖
pip install -r requirements.txt

# 配置环境变量
export DASHSCOPE_API_KEY="your_api_key_here"
# 或者创建.env文件
echo "DASHSCOPE_API_KEY=your_api_key_here" > .env

# 启动后端
python run.py
```

后端将在 **http://localhost:8000** 启动

### 步骤2：启动前端（新开终端）

```bash
# 进入前端目录
cd frontend

# 安装依赖
npm install

# 启动前端
npm run dev
```

前端将在 **http://localhost:3000** 启动

---

## 🎯 使用示例

### 1. 文生图

1. 在左侧菜单点击「文生图」
2. 输入提示词，例如："一只可爱的小猫咪坐在窗台上，夕阳西下"
3. 选择参数（模型、数量、尺寸）
4. 点击「开始生成」
5. 在右侧任务面板实时查看进度
6. 完成后自动显示结果

### 2. 图生视频

1. 点击「图生视频」
2. 上传一张图片
3. 输入动作描述（可选）
4. 选择分辨率和时长
5. 开始生成

### 3. 文生视频

1. 点击「文生视频」
2. 输入视频场景描述
3. 选择参数
4. 开始生成

### 4. 并发创作

✨ **可以同时创建多个任务！**

- 创建任务后无需等待
- 继续创建新任务
- 所有任务在后台并发执行
- 右侧面板实时显示所有任务进度

### 5. 历史记录

点击左侧「历史记录」查看所有创作历史，支持：
- 按类型筛选（文生图/文生视频/图生视频）
- 按状态筛选（等待中/生成中/已完成/失败）
- 删除不需要的记录

### 6. 灵感画廊

点击左侧「灵感画廊」：
- 浏览创作灵感示例
- 按分类查看（风景、人物、动物等）
- 一键复制提示词
- 直接使用提示词创作

---

## ⚙️ 环境变量配置

### 后端环境变量

环境变量优先级：**运行时环境变量 > .env文件**

```bash
# 推荐：运行时设置
export DASHSCOPE_API_KEY="your_key"
export HOST="0.0.0.0"
export PORT="8000"
export RELOAD="true"

# 或者创建backend/.env文件
cat > backend/.env <<EOF
DASHSCOPE_API_KEY=your_key_here
HOST=0.0.0.0
PORT=8000
RELOAD=true
DATABASE_URL=sqlite:///./qwenimg.db
EOF
```

### 前端环境变量（可选）

```bash
# 创建frontend/.env.local
cat > frontend/.env.local <<EOF
VITE_API_BASE=/api
VITE_WS_URL=
EOF
```

---

## 🐛 常见问题

### 前端白屏？

✅ **已修复！** 如果仍然遇到：

```bash
cd frontend
rm -rf node_modules package-lock.json
npm install
npm run dev
```

### 后端启动失败？

检查：
1. Python版本是否 ≥ 3.8
2. API Key是否正确设置
3. 端口8000是否被占用

```bash
# 检查端口
lsof -i :8000

# 修改端口
export PORT=8080
```

### WebSocket连接失败？

确保：
1. 后端已启动
2. 前端代理配置正确（vite.config.ts）
3. 防火墙未阻止WebSocket连接

### 任务一直pending？

1. 检查API Key是否有效
2. 查看后端日志
3. 检查网络连接

```bash
# 查看后端日志
tail -f logs/backend.log
```

---

## 📚 更多文档

- [完整文档](README_NEW_FRONTEND.md) - 详细使用指南
- [部署文档](DEPLOYMENT.md) - 生产环境部署
- [API文档](http://localhost:8000/api/docs) - 接口文档
- [原README](README.md) - 原始项目文档

---

## 🎉 开始创作吧！

现在你已经准备好了，打开 http://localhost:3000 开始你的AI创作之旅！

如有问题，请查看[完整文档](README_NEW_FRONTEND.md)或提交[Issue](https://github.com/cclank/qwenimg/issues)。
